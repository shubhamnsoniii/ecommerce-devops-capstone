apiVersion: v1
kind: Service

metadata:
  name: inventory-service
  namespace: shubham-ecommerce

spec:
  selector:
    app: inventory-service

  ports:
  - port: 5002
    targetPort: 5002

  type: NodePort