apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: shubham-ecommerce

spec:
  selector:
    app: frontend

  ports:
  - port: 80
    targetPort: 80
    nodePort: 30001

  type: NodePort