pipeline {
    agent any

    stages {

        stage('Checkout Verification') {
            steps {
                bat 'echo Jenkins has successfully checked out the source code.'
                bat 'dir'
            }
        }

    }
}